services:
  essence:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - RUST_LOG=essence=info
      - BROWSER_HEADLESS=true
      - BROWSER_POOL_SIZE=5
      - PORT=8080
    restart: unless-stopped
